//will display the ChevronLeft icon on the left if backIcon is true
//will display the Filter icon on the right if filterIcon is true
//will display a button (on the right) containing whatever text is passed to the "text" prop

export { TopNav } from './TopNav';
