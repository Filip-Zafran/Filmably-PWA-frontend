//takes a 'label' prop (string)
export { LetsStartButton } from './LetsStartButton';

//takes prop: 'like' (boolean) to determine if it's a like or dislike button
//takes prop: 'inactive' (boolean) to set styling if the other choice is activated
//takes prop: 'active' (boolean) to set styling if the corresponding choice is activated
//takes prop: 'clickHandler' for mouseDown function (to activate the choice)
//takes prop: 'otherHandler' for mouseUp function (to finalize the choice)
export { ShotsButton } from './ShotsButton';
